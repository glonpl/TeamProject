{% extends 'base.html.twig' %}

{% block title %}Symptoms index{% endblock %}

{% block body %}
    <h1>Symptoms index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Objaw</th>
                <th>Obszar</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for symptom in symptoms %}
            <tr>
                <td>{{ symptom.id }}</td>
                <td>{{ symptom.Name }}</td>
                <td>{{ symptom.getFKArea().Name }}</td>
                <td>
                    <a href="{{ path('symptoms_show', {'id': symptom.id}) }}">show</a>
                    <a href="{{ path('symptoms_edit', {'id': symptom.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('symptoms_new') }}">Create new</a>
{% endblock %}
