{% extends 'base.html.twig' %}

{% block title %}Symptoms{% endblock %}

{% block body %}
    <h1>Symptoms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ symptom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ symptom.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('symptoms_index') }}" class="btn btn-primary">Powrót</a>

    <a href="{{ path('symptoms_edit', {'id': symptom.id}) }}" class="btn btn-dark">Edytuj</a>

    {{ include('symptoms/_delete_form.html.twig') }}
{% endblock %}
