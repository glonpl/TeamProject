{% extends 'base.html.twig' %}

{% block title %}Area{% endblock %}

{% block body %}
    <h1>Area</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ area.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ area.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('area_index') }}" class="btn btn-primary">Powrót</a>

    <a href="{{ path('area_edit', {'id': area.id}) }}" class="btn btn-dark">Edytuj</a>

    {{ include('area/_delete_form.html.twig') }}
{% endblock %}
