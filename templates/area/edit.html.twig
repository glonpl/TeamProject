{% extends 'base.html.twig' %}

{% block title %}Edit Area{% endblock %}

{% block body %}
    <h1>Edit Area</h1>

    {{ include('area/_form.html.twig', {'button_label': 'Aktualizuj'}) }}

    <a href="{{ path('area_index') }}" class="btn btn-primary">Powrót</a>

    {{ include('area/_delete_form.html.twig') }}
{% endblock %}
